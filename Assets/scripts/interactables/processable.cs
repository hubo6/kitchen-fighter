using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class processable {
    // Start is called before the first frame update
    //[SerializeField] float _processTimeSpan = 1f;
    //[SerializeField] float _processCurrent = 0f;
    //[SerializeField] int _transformIdx = 0;

    //public event Action<float, float> onProcess;
    ////[SerializeField] Transform[] forms;
    //void Start()
    //{
    //    for (var i = 0; i < transform.childCount; i++)
    //        transform.GetChild(i).gameObject.SetActive(i == _transformIdx);
    //}

    //// Update is called once per frame
    //public virtual bool processUpdate(float process, INTERACT_TYPE type)  {
    //    if (_processCurrent == _processTimeSpan)
    //        return false;
    //    _processCurrent += process;
    //    if (_processCurrent >= _processTimeSpan) {
    //        _processCurrent = _processTimeSpan;
    //    }
    //    return true;

    //}
}
